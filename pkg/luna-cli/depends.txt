# luna-cli is a sane terminal out of the box.
fish                 # a shell that completes without being configured
starship             # the prompt
eza                  # ls
bat                  # cat with highlighting
ripgrep              # grep
fd                   # find
fzf                  # interactive search
zoxide               # a cd that learns
git-delta            # diff
btop                 # process monitor
yazi                 # file manager in the terminal
fastfetch            # system information
tealdeer             # tldr - examples instead of man
dust                 # du
duf                  # df
jq
tree
man-db
man-pages
neovim
git
wget
curl
unzip
p7zip
rsync

# --- from the AUR ---
# Built and signed by scripts/build-pkgs.sh out of pkg/aur.txt, and installed
# from the [luna] repository like everything else. It is listed as a dependency
# rather than named in the installer so that the package itself says what it
# needs. A system with no AUR helper cannot install an AUR helper without
# building one by hand, which is a silly place to leave somebody.
yay-bin              # AUR helper
