# Watch memory pressure in user sessions. Without this, running low on memory
# freezes the whole system instead of killing the one process that is eating it.
[Slice]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=50%
