# Сжатый swap в оперативной памяти. Быстрее файла подкачки на порядок и
# не изнашивает диск. Половина ОЗУ, но не больше 8 ГБ — дальше смысла нет.
[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
