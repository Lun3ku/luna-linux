# Compressed swap in RAM. An order of magnitude faster than a swap file, and
# it does not wear the disk out. Half of the RAM, but never more than 8 GB;
# beyond that there is no point.
[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
