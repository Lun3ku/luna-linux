# Показываем greetd на наш конфиг вместо /etc/greetd/config.toml,
# который принадлежит пакету greetd, — чтобы не переписывать чужой файл.
[Service]
ExecStart=
ExecStart=/usr/bin/greetd --config /etc/greetd/luna.toml
