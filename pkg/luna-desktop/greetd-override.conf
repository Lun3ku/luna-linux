# Point greetd at our config instead of /etc/greetd/config.toml, which belongs
# to the greetd package, so that somebody else's file is not overwritten.
[Service]
ExecStart=
ExecStart=/usr/bin/greetd --config /etc/greetd/luna.toml
