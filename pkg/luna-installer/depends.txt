# luna-installer is the TUI installer.
dialog               # the pseudo-GUI in the terminal
arch-install-scripts # pacstrap, genfstab, arch-chroot
gptfdisk             # sgdisk, for automatic partitioning
parted
dosfstools           # mkfs.fat for the ESP
btrfs-progs
util-linux           # cfdisk, lsblk

# The installer checks connectivity itself before touching the disk, and
# offers nmtui when there is none, so both are direct dependencies now.
curl
networkmanager
