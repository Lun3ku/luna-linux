# The live image: sudo without a password, because the user has no password to
# begin with. This file does not reach an installed system; there the rules are
# written by the installer and the password is asked for as usual.
luna ALL=(ALL:ALL) NOPASSWD: ALL
